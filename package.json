{
  "name": "Library",
  "version": "0.0.0",
  "description": "The chemX library is a light jquery like dom manuipilation library.",
  "main": "src/index.js",
  "scripts": {
    "build": "npm install && grunt",
    "start": "grunt watch",
    "test": "grunt"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-contrib-concat": "^0.5.0",
    "grunt-contrib-jshint": "^0.10.0",
    "grunt-contrib-uglify": "^0.5.1"
  }
}
